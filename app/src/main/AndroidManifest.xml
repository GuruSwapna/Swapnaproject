<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.enchanter21.socialdating">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:name=".App_Controller"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Signin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginSuccessActivity" />
        <activity android:name=".SwipeActivity" />
        <activity android:name=".prprofile" />
        <activity android:name=".Friendslist" />
        <activity
            android:name=".Homenav"
            android:label="@string/title_activity_homenav"
            android:parentActivityName=".Signin"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.enchanter21.socialdating.Signin" />
        </activity>
        <activity android:name=".NextActivity" />
        <activity android:name=".Likedlist"></activity>
    </application>

</manifest>